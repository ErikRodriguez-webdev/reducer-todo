//Initial State
export const allTodos = {
  item: "Learn about reducers",
  completed: false,
  id: 1
};

//Actions
export const addTodo = () => {};

//Reducer
export const reducer = (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
